# Monterey's UFS, ext4, framebuffer and USB gadget drivers are built in.
# Keep this file empty: listing built-in drivers makes modprobe report errors
# during the first initramfs stage without making those drivers load earlier.
